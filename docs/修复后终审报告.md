# OpenIris 修复后终审报告

**日期**: 2026-06-16  
**版本**: OpenIris v0.4.1-Alpha  
**审查员**: 主审 (Rawls 超时后自行完成)

---

## 审查结果: ✅ 通过

| 审查维度 | 结论 | 说明 |
|---------|------|------|
| R1: decoded 分支数据访问 | ✅ 通过 | `channel(i).row(k)[0]` 正确读取 h 维度数据 |
| R2: DFL 分支回归 | ✅ 通过 | `matat[4*reg_max+c]` 保持不变 |
| R3: 两阶段提取 | ✅ 通过 | `is_single_output = (output_blobs.size() == 1)` 正确 |
| R4: NMS 兼容性 | ✅ 通过 | `scale_unpad` 变换兼容两种格式 |
| R5: 标签一致性 | ✅ 通过 | 两端路径一致，回退链健壮 |
| R6: 导出流水线 | ✅ 通过 | `Optional` 导入已添加 |
| R7: 向后兼容 | ✅ 通过 | 内置模型 (3输出, num_w=144) 走 DFL 分支 |

## 关键修复验证

### 修复前 vs 修复后

| 项目 | 修复前 | 修复后 |
|------|--------|--------|
| 数据访问 | `matat[4]` (越界读取) | `channel(i).row(4)[0]` (正确读取) |
| 格式检测 | `num_w < 65` (多类模型误判) | `is_single_output` (基于输出数量) |
| 输出提取 | 单次循环 (out0/out1/out2) | 两阶段 (先提取再处理) |

### 模型输出验证

```
model.param → out0 = Concat(box[8400,4], sigmoid[8400,1], dim=0)
→ NCNN Mat: [c=8400, h=5, w=1]
→ channel(i).row(0)[0] = cx
→ channel(i).row(1)[0] = cy
→ channel(i).row(2)[0] = w
→ channel(i).row(3)[0] = h
→ channel(i).row(4)[0] = prob (sigmoid, 0-1)
```

## 构建信息

| 项目 | 值 |
|------|-----|
| 文件名 | `OpenIris-0.4.1-Alpha.apk` |
| 版本 | 0.4.1-Alpha |
| versionCode | 3 |
| 大小 | 26.4 MB |
| 签名 | release (openiris-release.jks) |

## 修改文件清单

| 文件 | 修改内容 |
|------|---------|
| `yolov11.cpp` | generate_proposals() decoded 分支修复 + runInference() 两阶段提取 |
| `export_pipeline.py` | Optional 导入 + model_meta.json + 文档修正 |
| `build.gradle` | 版本号更新 0.4.1-Alpha |

## 测试建议

1. 安装 APK → 导入 best_ncnn.zip → 上传测试图片
2. 验证: 标签显示 "pig" (非 "unknown")
3. 验证: 框图位置准确 (非左上角)
4. 验证: 置信度在 0-100% 范围内 (非 43814%)
5. 回归: 内置 COCO 模型推理正常

